# Gini.

UCI Adult 소득 데이터로 학습한 **블라인드 분류기**가 성별 격차를 어떻게 그대로 재현하는지를 두 단계에 걸쳐 감사한 프로젝트.
1단계에서 정적분(로렌츠 곡선·지니계수)으로 표준 지표의 *평균 불변성* 한계를 드러내고 집단 격차지수 `D`를 제안했고,
2단계에서 그 격차지수를 **미분**해 정확도와 공정성을 동시에 최적화하는 판정 임계값 `t*`를 해석적으로 도출·검증하였다.

> **수행② `지니계수가 숨긴 차별`** → **수행③ `공정성 적분 감사기`** 로 이어지는 연속 연구다.
> 앞 단계가 *"표준 지표는 차별을 못 본다"* 를 증명했다면, 뒤 단계는 *"그렇다면 어디를 잘라야 하는가"* 에 답한다.

---

## *Motive.*

활동지 3에서 정적분으로 로렌츠 곡선과 지니계수를 구하며, 사회의 불평등을 하나의 수로 압축하는 데 정적분을 쓸 수 있다는 것을 알게 되었다.
평소 관심 있던 인공지능과 연계해 두 가지 궁금증이 생겼다.

> **"AI 모델의 공정성도 이렇게 측정할 수 있는가? 그리고 그 측정을 신뢰할 수 있는가?"**

그래서 카드 E(지니계수를 다른 분야로 확장)와 카드 C(지니계수의 한계 비판 & 보완 지표 제안)를 결합하여,
소득 불평등 지표를 인공지능 형평성 측정으로 확장하고 그 한계까지 함께 직접 검증하기로 하였다.

대출·채용·복지 선별 등 AI 자동심사가 확산되며 알고리즘이 특정 집단을 구조적으로 불리하게 판정하는 **'디지털 약자'** 문제가 대두되고 있다.
그런데 보호변수(성별)를 아예 제외한 블라인드 모델조차 **대리변수 상관** 때문에 실제 격차를 그대로 재현한다.
학습 단계에서 민감 변수를 빼는 것만으로는 부족하고, 모델을 배포하기 전에 출력을 직접 뜯어보는 **사후 점검 도구**가 필요하다고 판단하였다.

## *Research Q & H.*

> 표준 불평등 지표 하나로 AI의 차별을 감사할 수 있는가? 없다면 무엇을 어떻게 재고, 그 측정값으로 모델을 어떻게 고칠 것인가?

- **가설 1 (대리변수)** — 보호속성(성별·인종)을 학습에서 완전히 제외해도, 혼인상태·근로시간 같은 대리변수를 통해 성별 정보가 간접적으로 흘러들어가 모델이 현실 격차와 유사한 결과를 만들 것이다.
- **가설 2 (평균 불변성)** — 단일 지니계수는 분포의 *집중도*만 측정하므로 집단 간 *평균 격차*에 둔감하고, 따라서 차별적인 모델을 '공정하다'고 오판할 것이다.
- **가설 3 (적분 격차지수)** — 두 집단 로렌츠 곡선 사이 넓이로 정의한 격차지수 `D`는 지니계수가 놓치는 격차를 직접 포착하며, 닫힌식 해와 실데이터 수치가 일치할 것이다.
- **가설 4 (미분 최적화)** — 정확도와 격차지수를 결합한 목적함수 `J(t)`의 1계 조건 `J'(t*)=0` 을 해석적으로 풀어 얻은 임계값은, 실데이터 격자탐색으로 얻은 임계값과 실질적으로 같은 성능을 낼 것이다.
- **가설 5 (trade-off)** — 임계값 조정만으로 완전 공정에 도달하려 하면 `t*`가 상한으로 포화되어 분류기가 사실상 무용해지는 trade-off가 존재할 것이다.

검증은 각각 ▲집단별 모델 평균 점수와 현실 기저율의 비교, ▲통합 지니계수의 무감각성, ▲`D`의 닫힌식 대 사다리꼴 적분값, ▲해석해 대 격자탐색의 정확도 차, ▲λ 스윕에 따른 `t*`·정확도·격차의 동시 추적으로 확인하였다.

## *Data.*

| 데이터 | 출처 | 규모 | 역할 |
|---|---|---|---|
| UCI Adult (Census Income) | UCI Machine Learning Repository | 32,561건 | 연소득 5만 달러 초과 여부 분류 공개 벤치마크 |
| 학습 분할 | 동일 | 21,113건 | 블라인드 로지스틱 회귀 학습 |
| 검증 분할 | 동일 | 9,049건 | 선택률·정확도·격차 감사 |

인구통계 특징으로 연소득 5만 달러 초과 여부를 맞히는 표준 벤치마크이며, 채용·대출·복지 심사 같은 **사람을 판정하는 자동심사**의 대리 과제로 삼기에 적합하다.

### *Preprocess.*

- **보호속성 제거** — 성별·인종을 학습 입력에서 **의도적으로 완전히 제외**하였다. "민감 변수를 안 쓰면 공정하다"는 통념을 실제로 시험하기 위한 설계다.
- **사용 특징** — 나이, 교육연수, 주당 근로시간, 직군, 학력, 혼인상태 등 6개 계열의 특징만 사용.
- **모델** — Logistic Regression (블라인드), 검증 **AUC = 0.900**.
- **자원의 정의** — 모델이 각 개인에게 부여한 고소득 예측 점수 `s = P(소득 > 50K)` 를 *분배되는 자원*으로 정의하였다. 소득 대신 '모델이 준 점수'를 분배 대상으로 놓는 순간, 소득 불평등 지표를 그대로 AI 감사에 이식할 수 있다.
- **집단 분할** — 성별(남/여)로 나누고, 집단별 점수의 누적분포로 로렌츠 곡선을 그렸다.

### *Group Stats.*

| 집단 | 인구비 `π` | 기저율 `p` (실제 > 50K) | 모델 평균 점수 |
|---|:---:|:---:|:---:|
| A (남) | 0.680 | 0.315 (30.4%) | 30.1% |
| B (여) | 0.320 | 0.108 (10.9%) | 11.5% |

**기저율 격차 +0.207** — 이것이 이후 모든 분석에서 드러나는 *불공정의 구조적 근원*이다.

---

## *Phase 1. 지니계수가 숨긴 차별*

### *Math.*

지니계수는 완전균등선 `y = x` 와 로렌츠 곡선 `L(x)` 사이 넓이 `A` 를 삼각형 넓이 `B(= 1/2)` 로 나눈 값이다.
로렌츠 곡선을 `L(x) = xⁿ` 으로 모델링하면 지니계수는 지수 `n` 하나로 정해진다.
(`n = 1` 이면 완전평등, `n` 이 커질수록 완전불평등에 접근한다.)

```
G = A/B = 2 ∫₀¹ ( x − L(x) ) dx        &        L(x) = xⁿ  ⇒  G = (n − 1)/(n + 1)
```

실제 데이터에서는 집단별 점수를 정렬·누적하여 **사다리꼴 적분**으로 `G` 를 구한 뒤 `n = (1 + G)/(1 − G)` 로 역산하였다.
그리고 이 지니계수가 놓치는 **집단 간 격차**를 포착하기 위해, 두 집단 로렌츠 곡선 사이 넓이를 새 지표 `D` 로 정의하였다.

```
D = ∫₀¹ | L_M(x) − L_F(x) | dx = | 1/(n_m + 1) − 1/(n_F + 1) |
```

곡선 사이 넓이라는 기하적 정의가 `xⁿ` 모델에서는 **닫힌식**으로 떨어진다는 점이 이 지표의 강점이다. 수치적분과 해석해를 서로 대조 검증할 수 있기 때문이다.

### *Result.*

| 집단 | 실제 > 50K | 모델 평균 점수 | 지니 (집단 내) | `n` |
|---|:---:|:---:|:---:|:---:|
| 남성 | 30.4% | 30.1% | 0.503 | 3.02 |
| 여성 | 10.9% | 11.5% | 0.624 | 4.32 |

- **통합 지니 = 0.569** — 전체를 한 덩어리로 보면 이 숫자 하나뿐이며, 여기서는 성차별의 근거를 전혀 찾을 수 없다.
- **평균 격차 = 18.6%p**, **격차지수 `D` = 0.064** — 집단을 나누는 순간 두 개의 증거가 새로 생긴다.
- 남성 집단의 곡선은 `n = 3.0` 부근으로, 공교롭게도 활동지에서 다룬 `L(x) = x³ (G = 0.5)` 와 거의 일치했다.
- 시각화상 두 로렌츠 곡선(남성 `G=0.50, n=3.0` / 여성 `G=0.62, n=4.3`) 사이에 뚜렷한 띠 모양의 넓이가 생기며, 그 면적이 곧 `D = 0.064` 이다.

### *Reading.*

성별과 인종을 학습에서 완전히 제외했는데도, 모델은 **여성 평균 11.5% / 남성 평균 30.1%** 로 현실 격차(**여성 10.9% / 남성 30.4%**)를 거의 그대로 그려냈다. → **가설 1 지지**

이어서 "이렇게 만들어진 차별을 흔히 쓰는 지니계수가 감지할 수 있는가?"를 따져본 결과, **불가능**하다는 결론에 도달했다.
지니계수는 분포의 집중도만 측정하고 평균에는 둔감하기 때문이다. **모든 점수를 `c` 배 해도 `G` 는 변하지 않는다.**
즉 한 집단의 점수를 통째로 절반으로 깎아도 지니계수는 미동도 하지 않는다. → **가설 2 지지**

`D` 는 `xⁿ` 모델의 닫힌식과 **0.06 수준에서 일치**하여 지표의 타당성이 확인되었다. → **가설 3 지지**
다만 모델의 `D`(0.064)는 현실 기저율로 계산한 `D`(0.097)보다 **작다**.
따라서 이 모델은 격차를 *증폭*시켰다기보다, **현실과 유사하게 결과를 만들어 놓고 표준지표로 은폐했다**고 보는 것이 정확하다.

---

## *Phase 2. 공정성 적분 감사기*

Phase 1이 남긴 문제는 명확했다. `D` 로 격차를 *잴 수는* 있게 되었지만, **그래서 임계값을 어디에 두어야 하는가**에는 답하지 못한다.
Phase 2는 이 측정 도구를 **미분 최적화**로 확장하여, 정확도와 공정성을 동시에 고려한 최적 임계값을 직접 계산하는 공학적 해결방안으로 마무리한다.

**설정** — 분류기 점수 `s ∈ [0,1]`, 임계값 규칙 `s ≥ t` 이면 1로 예측.
보호집단 A(남)·B(여), 인구비 `π_g`, 기저율 `p_g = P(y=1|g)`, 집단 및 클래스별 점수밀도 `f_{g,1}`(양성)·`f_{g,0}`(음성).

### *Formulation. — 적분*

선택률·격차지수·정확도를 모두 **점수밀도의 정적분(누적량)** 으로 정의한다.

```
SR_g(t) = ∫ₜ¹ f_g(s) ds ,        f_g = p_g · f_{g,1} + (1 − p_g) · f_{g,0}

Δ(t)   = SR_A(t) − SR_B(t) = ∫ₜ¹ [ f_A(s) − f_B(s) ] ds ,        D(t) = Δ(t)²

Acc(t) = Σ_g π_g [ p_g ∫ₜ¹ f_{g,1} ds + (1 − p_g) ∫₀ᵗ f_{g,0} ds ]
```

- **선택률 `SR_g(t)`** — 임계값 `t` 이상 점수의 누적. "집단 g에서 몇 %가 합격했는가".
- **격차지수 `D(t)`** — 두 집단 선택률 차의 제곱. 제곱으로 두어 부호에 무관하게 격차의 크기만 재고, 동시에 미분 가능하게 만든다.
- **정확도 `Acc(t)`** — 양성의 `TPR(∫ₜ¹ f_{g,1})` 과 음성의 `TNR(∫₀ᵗ f_{g,0})` 을 인구비 및 기저율로 가중한 값.

### *Differentiation. — 미적분의 기본정리(FTC)*

```
d/dt ∫ₜ¹ φ ds = −φ(t)        ,        d/dt ∫₀ᵗ φ ds = +φ(t)        (FTC)
```

FTC로 각 누적량을 `t` 에 대해 미분하면 **경계에서의 밀도값만 남는다.** 적분으로 쌓아 올린 지표가 미분 한 번에 국소 밀도로 환원되는 것이 이 설계의 핵심이다.

```
Acc′(t) = Σ_g π_g [ (1 − p_g) f_{g,0}(t) − p_g f_{g,1}(t) ]

D′(t)  = 2 Δ(t) Δ′(t) = −2 Δ(t) [ f_A(t) − f_B(t) ]
```

`D′` 는 **연쇄법칙**으로 얻으며, 그 안의 `Δ′(t) = −[ f_A(t) − f_B(t) ]` 는 다시 FTC다. 즉 하나의 도함수 안에 연쇄법칙과 기본정리가 겹쳐 들어간다.

### *Optimization. — 1계 조건*

목적함수 `J(t) = Acc(t) − λ D(t)` (λ는 공정성 가중치)를 미분하면,

```
J′(t) = Acc′(t) + 2 λ Δ(t) [ f_A(t) − f_B(t) ] = 0
```

좌변(**정확도 기울기**)과 우변(**공정성 이득**)이 균형을 이루는 `t` 가 최적 임계값 `t*` 다.
특히 `λ = 0` 이면 고전적 **베이즈 임계값**으로 환원되어 모형의 타당성이 확인된다.

```
λ = 0 :  Acc′(t*) = 0  ⇒  Σ_g π_g p_g f_{g,1}(t*) = Σ_g π_g (1 − p_g) f_{g,0}(t*)
```

### *Logit-Normal. — 구체화*

LR 로짓 `z = logit(s)` 가 근사적으로 정규 `N(μ, σ²)` 를 따르므로 점수는 **로짓정규분포**를 갖는다.
이때 선택률이 정규 CDF `Φ` 로 닫힌 형태가 되어 연쇄법칙 미분이 깔끔하게 정리된다.

```
SR_{g,y}(t) = Φ( ( μ_{g,y} − logit t ) / σ_{g,y} ) ,        logit t = ln( t / (1 − t) )

f_{g,y}(t) = − d/dt SR_{g,y}(t) = φ( (logit t − μ) / σ ) / ( σ · t · (1 − t) )
```

여기서 결정적인 따름정리가 하나 나온다. **클래스조건부 밀도가 집단 불변**일 때 격차지수는 **기저율 차의 제곱에 비례**한다.

```
D(t) = ( p_A − p_B )² [ ∫ₜ¹ ( f₁ − f₀ ) ds ]²    ⇒    D ∝ ( p_A − p_B )²
```

즉 차별의 근원이 모델의 결함이 아니라 **기저율 격차 `(p_A − p_B)²`** 라는 사실을 수식으로 직접 볼 수 있다.
Phase 1에서 관측된 기저율 격차 **+0.207** 이 왜 임계값을 아무리 흔들어도 사라지지 않는지가 여기서 설명된다.

### *Fitting.*

로짓정규 적합 결과 `(μ, σ)`:

| 집단·클래스 | μ | σ |
|---|:---:|:---:|
| A1 (남·양성) | +0.78 | 2.39 |
| A0 (남·음성) | −2.63 | 2.12 |
| B1 (여·양성) | +0.47 | 2.39 |
| B0 (여·음성) | −3.58 | 1.74 |

### *Result. — λ 스윕*

| λ | `t*` (해석해) | `t*` (격자탐색) | Acc @ 해석 `t*` | Acc @ 격자 `t*` | \|ΔAcc\| | DP 격차 |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 0 | 0.5633 | 0.4703 | 0.8442 | 0.8478 | 0.0036 | +0.1491 |
| 1 | 0.7041 | 0.6069 | 0.8300 | 0.8436 | 0.0136 | +0.0987 |
| 3 | 0.8407 | 0.7485 | 0.8044 | 0.8235 | 0.0191 | +0.0529 |
| 10 | 0.9516 | 0.8880 | 0.7806 | 0.7956 | 0.0149 | +0.0270 |
| 30 | 0.9800 | 0.9851 | 0.7710 | 0.7700 | 0.0010 | +0.0182 |
| 100 | 0.9800 | 0.9851 | 0.7710 | 0.7700 | 0.0010 | +0.0182 |

*표 1. 공정성 가중치 λ에 따른 최적 임계값과 정확도·격차. 해석해(로짓정규 모형)와 격자탐색(실데이터 직접)의 정확도가 최대 1.9%, 대부분 1% 이내로 일치.*

**① λ = 0 에서 `Acc′(t*) ≈ 4.6 × 10⁻¹⁷ ≈ 0`**
→ 해석해가 **베이즈 정확도 최적 임계값으로 정확히 환원**된다. 수치적으로 0에 해당하는 값이 나왔다는 것은 유도된 1계 조건이 옳다는 강한 증거다.

**② λ를 0 → 100 으로 키우면 `t*`가 0.56 → 0.98 로 이동하며 DP 격차가 0.149 → 0.018 로 감소**
→ 그 대가로 정확도가 **0.844 → 0.771** 로 하락하여, 정확도와 공정성의 trade-off가 정량화되었다.

**③ 격차를 0.149 → 0.018 로 줄이는 데 정확도 약 7%p가 소모되고 `t*`가 0.98로 포화**
→ 임계값 조정만으로의 완전공정은 사실상 **분류 무용 상태**임을 확인할 수 있다. → **가설 5 지지**

### *Validation. — 수식 ↔ 시뮬레이션*

해석해 `t*` 에서의 정확도와 실데이터 격자탐색 `t*` 에서의 정확도가 **모든 λ에서 1.9% 이내로 일치**하였다. → **가설 4 지지**

`t*` **위치 자체는** λ가 작은 구간에서 다소 차이가 난다(예: λ=3에서 0.8407 대 0.7485).
이는 오차가 아니라, **정확도 목적함수가 최적점 부근에서 평평(flat)하기 때문**이다.
평평한 봉우리 위에서는 위치가 꽤 달라도 높이(정확도)는 거의 같으므로, *성능이 일치한다*는 사실이 *위치가 일치한다*는 사실보다 중요한 검증 기준이 된다.

시각화로도 같은 결론이 확인된다.
- **그림 1(a)** — `J(t) = Acc(t) − λD(t)` 가 λ=3에서 `t* = 0.841` 의 **내부 최댓값**을 가진다. 같은 축 위에서 `Acc(t)`는 완만한 봉우리를, `D(t) = Δ²`는 낮은 임계값 구간에서 급격히 솟았다가 감소하는 형태를 보인다.
- **그림 1(b)** — `J′(t)` 가 `t*` 에서 **정확히 0을 통과**한다(1계 조건의 시각적 확인).
- **그림 2(좌)** — 정확도–공정성 **프런티어**. `|DP gap|`을 0.15에서 0.02 쪽으로 줄여갈수록 `t*`에서의 정확도가 0.844에서 0.77대로 단조 하락한다.
- **그림 2(우)** — `t*(λ)` 곡선. λ가 커질수록 `t*`가 상승하다가 λ ≈ 10 이후 **0.98에서 포화**된다.

## *Pipeline.*

Python(`scipy` / `scikit-learn` / `matplotlib`)으로 전 과정을 자동화하였다.

| 단계 | 내용 |
|:---:|---|
| ㉠ | 블라인드 LR 학습 (보호변수 성별·인종 제외, AUC 0.900) |
| ㉡ | 집단·클래스별 점수밀도 **로짓정규 적합** |
| ㉢ | `J′(t*) = 0` **해석해** 계산 (`brentq` 구간 이분법) |
| ㉣ | 실데이터 **격자탐색** 검증 |
| ㉤ | **λ 스윕**·시각화 |

해석해를 구간 이분법(`brentq`)으로 푸는 것은 1계 조건이 `t*` 부근에서 부호를 바꾸는 연속함수라는 성질을 그대로 이용한 것이며,
같은 값을 격자탐색으로 한 번 더 구해 **수식과 시뮬레이션을 교차 검증**하는 구조로 설계하였다.

## *Conclusion*

1. **차별은 보호속성을 제외해도 사라지지 않는다.** 혼인상태·근로시간 같은 대리 변수가 성별과 상관을 가져, 성별을 직접 넣지 않아도 성별 정보가 모델에 간접적으로 흘러 들어갔다. "민감 변수를 안 쓰면 공정하다"는 통념은 틀렸다.
2. **지니계수 하나로는 성차별이 보이지 않는다.** 평균 불변성 때문이다. 단일 지니계수로 AI 공정성을 감사하면 차별적인 모델을 '공정하다'고 오판할 수 있다. (통합 지니 0.569 — 이 숫자만으로는 아무 근거도 없다.)
3. **정적분 기반 집단 격차지수 `D` 를 병행해야 한다.** `D` 는 두 집단 로렌츠 곡선 사이 넓이로 격차를 직접 포착하며, `xⁿ` 모델의 닫힌식과 0.06 수준에서 일치해 타당성이 확인되었다.
4. **모델은 격차를 증폭한 것이 아니라 은폐했다.** 모델 `D`(0.064)가 현실 기준 `D`(0.097)보다 작다는 것은, 현실과 유사한 결과를 만들어 놓고 표준지표 뒤에 숨겼다는 뜻으로 읽는 것이 정확하다.
5. **차별의 근원은 기저율 격차다.** 클래스조건부 밀도가 집단 불변이면 `D ∝ (p_A − p_B)²`. 관측된 기저율 격차 +0.207이 모든 격차의 상수항처럼 작동한다.
6. **적분으로 재고 미분으로 고친다.** `J′(t*) = 0` 의 해석해가 λ=0에서 베이즈 임계값으로 정확히 환원(`Acc′(t*) ≈ 4.6×10⁻¹⁷`)되고, 실데이터 격자탐색과 정확도 1.9% 이내로 일치하여 모형의 타당성이 이중으로 확인되었다.
7. **완전공정은 공짜가 아니다.** DP 격차를 0.149 → 0.018로 줄이는 데 정확도 약 7%p가 소모되고 `t*`는 0.98로 포화된다. 임계값 조정만으로 완전공정에 도달하면 분류기 자체가 무용해진다.

결론적으로 인공지능 형평성 감사의 표준 지표에 **'집단 격차 적분 지표'** 를 포함하고, 데이터 단계에서 대리변수를 재차 확인해야 한다.
이를 통해 채용·대출·복지 심사처럼 AI가 약자에게 불이익을 줄 수 있는 영역에서 차별을 사전에 차단할 수 있다.

## *Limit & Plan*

- **분포 가정의 근사오차** — 점수분포를 로짓정규로 가정한 데서 오는 근사오차가 존재한다. 해석해와 격자탐색의 `t*` 위치 차이 중 일부는 여기서 비롯된다.
- **임계값 단독 개입의 한계** — 후처리(임계값 조정)만으로는 기저율 격차라는 구조적 근원을 건드릴 수 없다. `D ∝ (p_A − p_B)²` 가 이를 수식으로 보여준다. 전처리(재가중)·학습 중 제약(fairness constraint)과의 병행이 필요하다.
- **단일 공정성 기준** — 본 연구는 선택률 격차(Demographic Parity 계열) 하나만 최적화했다. 기회 균등(Equal Opportunity)·균등 오즈(Equalized Odds)를 동시에 만족시킬 수 없다는 불가능성 정리까지 함께 검토해야 한다.
- **단일 보호속성** — 성별 하나만 감사하였다. 인종·연령과의 교차(intersectional) 격차는 다루지 못했다.
- **활용 방안** — 채용·대출·복지 심사 AI의 **배포 전 공정성 감사 표준 절차**로 사용한다. λ를 조직의 정책 파라미터로 두면, 표 1의 프런티어가 곧 "우리는 공정성 얼마를 사기 위해 정확도 얼마를 지불할 것인가"라는 의사결정표가 된다.

---

<details>
<summary><b>*Appendix.*</b></summary>

### *Notation.*

| 기호 | 유형 | 설명 |
|---|---|---|
| `s` | 수치 | 분류기 점수, `s ∈ [0,1]`, `s = P(소득 > 50K)` |
| `t` | 수치 | 판정 임계값. `s ≥ t` 이면 1로 예측 |
| `g` | 범주 | 보호집단. A(남) / B(여) |
| `π_g` | 수치 | 집단 `g` 의 인구비 (A 0.680 / B 0.320) |
| `p_g` | 수치 | 집단 `g` 의 기저율 `P(y=1\|g)` (A 0.315 / B 0.108) |
| `f_{g,1}` / `f_{g,0}` | 함수 | 집단·클래스별 점수밀도 (양성 / 음성) |
| `f_g` | 함수 (파생) | 집단 전체 밀도 `p_g f_{g,1} + (1−p_g) f_{g,0}` |
| `SR_g(t)` | 수치 (적분) | 선택률 `∫ₜ¹ f_g ds` |
| `Δ(t)` | 수치 (파생) | 선택률 차 `SR_A − SR_B` |
| `D(t)` | 수치 (파생) | 격차지수 `Δ(t)²` |
| `Acc(t)` | 수치 (적분) | 정확도 (TPR·TNR의 인구·기저율 가중합) |
| `J(t)` | 함수 | 목적함수 `Acc(t) − λ D(t)` |
| `λ` | 수치 | 공정성 가중치 (0 = 정확도만, ∞ = 공정성만) |
| `t*` | 수치 | `J′(t*) = 0` 을 만족하는 최적 임계값 |
| `L(x)` | 함수 | 로렌츠 곡선. `xⁿ` 으로 모델링 |
| `G` | 수치 (적분) | 지니계수 `2∫₀¹(x − L(x))dx = (n−1)/(n+1)` |
| `n` | 수치 (역산) | `L(x) = xⁿ` 의 지수, `n = (1+G)/(1−G)` |
| `D` (Phase 1) | 수치 (적분) | 두 로렌츠 곡선 사이 넓이 `\|1/(n_m+1) − 1/(n_F+1)\|` |
| `μ`, `σ` | 수치 | 로짓 `z = logit(s)` 의 정규 적합 모수 |

### *Term.*

- **로렌츠 곡선** — 하위 `x` 비율이 전체 자원의 몇 %를 갖는지 나타내는 누적 곡선. 완전균등이면 `y = x`
- **지니계수** — 완전균등선과 로렌츠 곡선 사이 넓이를 삼각형 넓이로 나눈 값. 0이면 완전평등, 1에 가까울수록 불평등
- **평균 불변성** — 모든 값을 `c` 배 해도 지니계수가 변하지 않는 성질. 집단 간 평균 격차를 감지하지 못하는 원인
- **대리변수(Proxy)** — 보호속성과 상관이 높아, 보호속성을 제외해도 그 정보를 간접적으로 전달하는 변수 (예: 혼인상태·근로시간 ↔ 성별)
- **블라인드 모델** — 보호속성을 학습 입력에서 제외한 모델. 공정성이 자동으로 보장되지는 않는다
- **선택률(Selection Rate)** — 특정 집단에서 임계값 이상 점수를 받아 '합격' 판정된 비율
- **기저율(Base Rate)** — 집단 내 실제 양성 비율 `P(y=1|g)`. 격차의 구조적 근원
- **DP 격차(Demographic Parity gap)** — 두 집단 선택률의 차. 0이면 인구통계적 동등
- **미적분의 기본정리(FTC)** — 적분의 상·하한을 변수로 미분하면 경계에서의 피적분함수 값만 남는다는 정리
- **연쇄법칙** — 합성함수의 미분법. `D = Δ²` 의 도함수 `2ΔΔ′` 를 얻는 데 사용
- **로짓정규분포** — 로짓 변환값이 정규분포를 따르는 `[0,1]` 위의 분포. 선택률이 정규 CDF `Φ` 로 닫힌다
- **베이즈 임계값** — 정확도를 최대화하는 고전적 최적 임계값. `λ = 0` 일 때의 특수해
- **brentq** — 구간 내 부호 변화를 이용해 방정식의 근을 찾는 이분법 계열 수치해법
- **격자탐색(Grid Search)** — 임계값을 촘촘히 훑으며 실데이터에서 직접 최적점을 찾는 방식. 해석해의 검증용 대조군
- **정확도–공정성 프런티어** — 격차를 줄일수록 정확도가 떨어지는 관계를 그린 곡선. 정책 선택의 메뉴판 역할
- **trade-off 포화** — λ를 아무리 키워도 `t*` 가 상한(0.98)에서 더 움직이지 않고 정확도만 잃는 구간

</details>
