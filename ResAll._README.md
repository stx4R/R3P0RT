# ResAll.

코스타리카 가구 빈곤 수준 데이터로 **"한정된 복지 예산을 누구에게 배분할 것인가"** 를 분류 문제로 구현한 프로젝트.
Orange에서 결정 트리 · 랜덤 포레스트 · 로지스틱 회귀 세 모델을 학습·비교하고,
기본 패키지에 없는 **Fairness 애드온을 직접 설치**해 가구주 성별·지역을 보호속성으로 하는 **알고리즘 공정성 감사**와 **편향 완화(Reweighing)** 까지 수행하였다.

---

## *Motive.*

복지 예산은 한정되어 있고, **정작 가장 가난한 가구일수록 소득을 공식적으로 증빙하기 어렵다.**
이를 해결하기 위해 미주개발은행(IADB)이 **대리소득조사(Proxy Means Test, PMT)** — 소득 대신 주거·자산처럼 관측 가능한 특성으로 빈곤을 추정하는 방식 — 를 사용한다는 점에 착안해, 머신러닝 분류 문제로 접근했다.

### *Purpose.*

한정된 복지 예산을 가장 도움이 필요한 가구에 정확히 배분하기 위해, 소득 증빙이 어려운 상황에서도 **관측 가능한 가구 특성(주거·자산·교육 등)만으로 빈곤 취약 가구를 식별**하는 분류 모델을 구현한다.
나아가 이 모델이 **특정 집단(여성 가구주, 농촌 가구 등)을 체계적으로 놓치지 않는지** 알고리즘 공정성을 검토하여, 기술이 기존의 사회 불평등을 재생산하지 않도록 **책임 있게 설계**하는 것을 목적으로 한다.

## *Research Q & H.*

> 관측 가능한 가구 특성만으로 빈곤 취약 가구를 식별할 수 있는가? 그리고 **정확한 모델은 곧 공정한 모델인가?**

- **가설 1 (식별 가능성)** — 소득 변수 없이도 주거·자산·교육·가구구성 특성만으로 취약/비취약을 분류할 수 있을 것이다.
- **가설 2 (지표 선택)** — 복지 맥락에서는 '도움이 필요한데 놓친 가구(위음성)'의 비용이 '잘못 지원한 경우(위양성)'보다 크므로, 정확도(CA)가 아니라 **Recall·F1** 이 핵심 지표가 되어야 한다.
- **가설 3 (편향의 출발점)** — 모델을 만들기 **전부터** 데이터 자체에 집단 편향(DI·SPD)이 존재할 것이다. 즉 편향의 출발점은 알고리즘이 아니라 사회·데이터다.
- **가설 4 (trade-off)** — Reweighing으로 편향을 완화하면 정확도를 일부 양보하는 대신 공정성 지표가 개선되며, **여러 공정성 기준을 한꺼번에 만족시킬 수는 없을** 것이다.

## *Data.*

| 항목 | 내용 |
|---|---|
| 수집 데이터 | **코스타리카 가구 빈곤 수준 데이터** (Costa Rican Household Poverty Level Prediction – IADB) |
| 수집 방법 | 미주개발은행이 복지 대상 선정에 사용하는 **대리소득조사(PMT)** 의 정확도를 높이기 위해 수집·공개한 데이터. Kaggle에서 `train.csv` 를 내려받아 Orange의 **File 위젯**으로 적재 |
| 전체 속성 | 총 **142개 예측 변수 + Target** |

**속성 구성**

- **주거 환경** — 벽·지붕·바닥 재질, 전기·수도·화장실
- **자산 보유** — 냉장고·TV·컴퓨터·태블릿·휴대폰
- **가구 구성** — 가구원 수, 아동·고령 수, 과밀도
- **교육** — 평균 교육연수, 가구주 교육연수
- **인구** — 나이·성별
- **지역** — 도농, 6개 권역 등

### *Features.*

빈곤과 직결되고 **해석 가능한** 약 20개 변수를 선별하였다.

| 계열 | 변수 |
|---|---|
| 주거 | `epared1~3`(벽), `etecho1~3`(지붕), `eviv1~3`(바닥), `abastaguadentro`(수도), `noelec`(전기없음), `v14a`(화장실) |
| 과밀·규모 | `rooms`, `overcrowding`, `hacdor`, `tamhog`, `hogar_nin`, `hogar_mayor` |
| 자산 | `refrig`, `computer`, `television`, `qmobilephone`, `v18q` |
| 교육·인구 | `meaneduc`, `escolari`, `age`, `male`(가구주 성별) |
| 지역 | `area1`(도시)·`area2`(농촌), `lugar1~6`(권역) |

> ※ 문자·숫자 **혼합 변수**(`edjefe`·`edjefa`·`dependency`)와 **식별자**(`Id`·`idhogar`)는 학습에서 제외(meta 처리)하여 **데이터 누수를 차단**하였다.

### *Target.*

`Target`(빈곤 수준). 원래 **4단계**(1 = 극빈, 2 = 중간빈곤, 3 = 취약, 4 = 비취약)를 **'지원 필요 여부'** 로 이진화하였다.

```
취약 (1 · 2 · 3)   vs   비취약 (4)
```

복지 **'대상 식별'** 관점에 맞추고, 공정성 분석을 명료하게 하기 위함이다.

### *Preprocess.*

| 단계 | 위젯 | 처리 내용 |
|:---:|---|---|
| ① | **Select Rows** | 가구주(`parentesco1 = 1`) 행만 추출 → **한 행 = 한 가구** (개인 중복 제거, 약 3,000가구) |
| ② | **Edit Domain** | `Target` 을 취약/비취약 2클래스로 병합, `male` 을 남성/여성으로 라벨링(**보호속성 지정**) |
| ③ | **Select Columns** | 독립변수 약 20개 선택, 혼합형 변수·식별자 제외 |
| ④ | **Impute** | 결측치를 평균/최빈값으로 대체 |
| ⑤ | **As Fairness Data** | 보호속성 = 가구주 성별, 특권집단 = 남성, 유리한 결과 = 비취약으로 지정하여 **공정성 분석 기반 마련** |

### *Split.*

| 구분 | 역할 |
|---|---|
| **훈련 데이터 (70%)** | 모델이 가구 특성과 빈곤 여부 사이의 패턴을 학습하는 데 사용. **Data Sampler** 로 전체의 70%를 **계층 추출**하여 클래스·집단 비율을 유지한 채 학습에 투입 |
| **테스트 데이터 (30%)** | 학습에 사용하지 않은 나머지 30%. 모델이 처음 보는 데이터에 얼마나 잘 **일반화**되는지(과적합 여부)를 객관적으로 평가하는 데 사용 |

Data Sampler 설정 — `Fixed proportion of data: 70%`, **Replicable(deterministic) sampling** 및 **Stratify sample** 체크.
전처리 후 2,973건이 **훈련 2,082 / 테스트 891** 로 분리되었다.

## *Modeling.*

| 모델 | 유형 | 개념 및 작동 원리 |
|---|---|---|
| **결정 트리** (Decision Tree) | 규칙 기반 | 데이터를 가장 잘 나누는 질문을 반복해 트리로 분기한다. 각 분기는 **정보 이득이 최대**가 되도록 선택되며, 이는 **분할 전후 엔트로피의 감소량**으로 계산된다. 결과를 사람이 읽을 수 있어 *'어떤 요인이 빈곤을 가르는가'* 를 해석할 수 있는 것이 강점 |
| **랜덤 포레스트** (Random Forest) | 앙상블 (배깅) | 서로 다른 데이터·변수 부분집합으로 학습한 여러 결정 트리를 만들고, 그 예측을 **다수결로 종합**한다. 개별 트리의 과적합·분산을 평균으로 상쇄해 일반화 성능과 안정성이 높다. 단일 트리보다 정확하지만 **해석이 어려워지는 trade-off** 가 있다 |
| **로지스틱 회귀** (Logistic Regression) | 선형·확률 | 각 변수에 가중치를 곱해 더한 값을 **시그모이드 함수**에 넣어 0~1의 '빈곤 확률'을 출력한다. 결정 경계가 선형이며, 각 변수의 **계수**로 *'어떤 요인이 빈곤 확률을 얼마나 높이거나 낮추는가'* 를 **방향까지** 해석할 수 있다. 트리 계열과 다른 패러다임이라 비교 기준으로 적합 |

### *Workflow.*

```
File → Select Rows → Edit Domain → Select Columns → Impute ─┐
                                                            ├→ Test and Score → Box Plot
As Fairness Data → Data Sampler ────────────────────────────┤                 → Confusion Matrix
        │                                                   │
        ├→ Dataset Bias                          Random Forest / Tree /
        └→ Reweighing → Weighted Logistic Regression         Logistic Regression
```

- **Data Sampler** 의 `Data Sample → Data`, `Remaining Data → Test Data` 를 **Test and Score** 에 연결하여 시간·집단 비율을 보존한 채 평가.
- **Dataset Bias** 위젯으로 모델 학습 **이전**의 데이터 자체 편향을 측정.
- **Reweighing** 을 전처리기로 물린 **Weighted Logistic Regression** 을 편향 완화 모델로 추가.

## *Result.*

### *성능 비교 (테스트 데이터 30% 기준)*

| 모델 | AUC | CA | F1 | Prec | Recall | MCC |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| **로지스틱 회귀** | **0.852** | **0.783** | **0.776** | **0.778** | **0.783** | **0.501** |
| 랜덤 포레스트 | 0.829 | 0.746 | 0.738 | 0.738 | 0.746 | 0.413 |
| 결정 트리 | 0.624 | 0.703 | 0.702 | 0.702 | 0.703 | 0.338 |

### *공정성 지표 (보호속성 = 가구주 성별)*

| 모델 | SPD | EOD | AOD | DI |
|---|:---:|:---:|:---:|:---:|
| 로지스틱 회귀 | −0.076 | −0.046 | −0.066 | 0.899 |
| 랜덤 포레스트 | −0.050 | −0.017 | −0.045 | 0.933 |
| 결정 트리 | **−0.027** | **−0.024** | **−0.011** | **0.960** |

> 성능이 가장 좋은 로지스틱 회귀가 **공정성 지표에서는 가장 나쁘고**, 성능이 가장 낮은 결정 트리가 **공정성 지표에서는 가장 좋다.**
> **정확한 모델이 곧 공정한 모델은 아니라는 사실**이 같은 표 안에서 그대로 드러난다.

### *평가*

**① 정확도(CA)만으로 판단하지 않는다.**
취약/비취약이 불균형하면 다수 클래스만 찍어도 CA가 높게 나올 수 있다.

**② 복지 맥락에서는 Recall이 가장 중요하다.**
실제 취약 가구를 놓치면(**위음성**) 지원에서 배제되기 때문이다. 따라서 **Recall · F1**(정밀도와 재현율의 조화평균)을 우선 비교한다. → **가설 2 채택**

**③ 로지스틱 회귀가 CA·F1 모두 가장 높았고, 랜덤 포레스트, 결정 트리 순이었다.**
이진 더미 변수가 많은 표 데이터에서는 **선형 모델이 잘 맞는 경우가 많고**, 랜덤 포레스트는 **트리 수(10개)가 적어** 상대적으로 낮게 나온 것으로 보인다.
(결정 트리는 **해석 가능성**, 로지스틱 회귀는 **확률 출력**의 강점을 가진다는 점을 고려하여 해석하였다.)

**④ 공정성 지표(DI·SPD·AOD·EOD)도 함께 확인한다.**
**Reweighing** 을 적용한 **Weighted Logistic Regression** 이 일반 모델 대비 공정성이 개선되는지(정확도를 약간 양보하더라도) 비교한다.

### *Fairness Audit.*

기본 Orange에는 포함되지 않은 **Fairness 애드온을 직접 설치**해 공정성을 정량적으로 감사하였다.

1. **보호속성 설정** — 가구주 성별과 지역(도농) **두 가지**를 보호속성으로 지정.
2. **Dataset Bias** — 모델을 만들기 **전부터** 데이터 자체에 편향(DI·SPD)이 존재함을 확인하여, **편향의 출발점이 사회·데이터에 있음**을 드러냈다. → **가설 3 지지**
3. **편향 완화** — **Reweighing** 기법과 **Weighted Logistic Regression** 으로 편향을 완화한 모델을 직접 구현해, 완화 전후의 공정성 지표(DI·SPD·AOD·EOD)와 정확도를 비교하였다.

그 결과 **정확도를 일부 양보하는 대신 공정성이 개선되는 trade-off** 를 정량적으로 관찰했고, 동시에 **여러 공정성 기준을 한꺼번에 만족시킬 수는 없다**는 한계까지 확인하였다. → **가설 4 지지**
이는 **편향 완화가 설계자가 책임지고 내려야 할 선택**임을 보여준다.

> 참조) Orange 파일 두 개를 이용하여 지역(도시·시골) 보호속성을 분석하였으며, 그중 **도시** Orange 파일을 기준으로 스크랩본·파일을 첨부하였다.

## *Conclusion*

1. **소득 없이도 빈곤은 추정 가능하다.** 주거·자산·교육·가구구성만으로 취약/비취약을 CA 0.783 (F1 0.776, AUC 0.852) 수준으로 분류하였다. → **가설 1 지지**
2. **지표는 맥락이 정한다.** 복지에서는 '도움이 필요한데 놓친 가구'의 비용이 '잘못 지원한 경우'보다 크므로, CA가 아닌 **Recall·F1** 을 핵심 지표로 삼아야 한다.
3. **정확한 모델 ≠ 공정한 모델.** 성능 1위 로지스틱 회귀가 공정성 지표에서는 최하위였다. 성능표 하나만 보고 모델을 고르면 차별을 그대로 배포하게 된다.
4. **편향의 출발점은 알고리즘이 아니라 데이터다.** Dataset Bias 위젯으로 학습 이전 단계에서 이미 DI·SPD 편향이 존재함을 확인하였다.
5. **완화는 공짜가 아니며, 모두를 만족시킬 수도 없다.** Reweighing은 정확도를 양보한 만큼 공정성을 얻었고, 여러 공정성 기준을 동시에 만족시키는 것은 불가능했다. **편향 완화는 설계자가 책임지고 내려야 할 선택**이다.
6. **분석의 타당성을 스스로 점검하며 설계했다.** 가구주 단위로 개인 중복을 제거하고, 4단계 등급을 '지원 필요 여부'로 이진화했으며, 문자·숫자 혼합 변수와 식별자를 배제해 **데이터 누수를 차단**하였다.
7. **주어진 과제를 넘어선 설계 경험.** 본 탐구는 분류 과제를 기계적으로 수행하는 데 머물지 않고, **데이터 정제 → 모델 비교 → 공정성 감사 → 편향 완화**까지 일련의 과정을 스스로 설계하며 진행하였다. 이를 통해 주변 사회의 문제를 공학적으로 해결하되 **그 기술이 미칠 윤리적 영향까지 책임지는 AI 설계자의 태도**를 간접적으로 경험할 수 있었다.

## *Limit & Plan*

- **용도의 한정** — 예측 결과는 **낙인이나 처벌이 아니라 지원 대상 발굴**에 한정해 쓰여야 하며, 최종 판단에는 반드시 **사람의 검토**가 따라야 한다.
- **지역 일반화의 한계** — 본 모델은 **코스타리카 데이터**로 학습된 것이라 다른 사회에 그대로 적용하기 어렵다. **지역별 재학습**과 **지속적인 공정성 모니터링**이 필요하다.
- **모델 하이퍼파라미터** — 랜덤 포레스트의 트리 수가 10개로 적어 성능이 과소평가되었을 가능성이 있다. 트리 수·깊이 탐색으로 재비교가 필요하다.
- **공정성 기준의 선택** — DI·SPD·AOD·EOD를 동시에 만족시킬 수 없으므로, **어느 기준을 우선할 것인가**에 대한 정책적 합의가 기술적 튜닝보다 먼저 이루어져야 한다.

---

<details>
<summary><b>*Appendix.*</b></summary>

### *Main Variables.*

| 변수 | 유형 | 설명 |
|---|---|---|
| `Target` | 범주 (타깃) | 빈곤 수준. 4단계 → **취약(1·2·3) / 비취약(4)** 이진화 |
| `parentesco1` | 이진 (필터) | 가구주 여부. `= 1` 인 행만 추출해 한 행 = 한 가구로 정규화 |
| `male` | 이진 (**보호속성**) | 가구주 성별. 특권집단 = 남성 |
| `area1` / `area2` | 이진 (**보호속성**) | 도시 / 농촌 |
| `lugar1~6` | 이진 | 6개 권역 |
| `epared1~3` / `etecho1~3` / `eviv1~3` | 이진 | 벽 / 지붕 / 바닥 재질 등급 |
| `abastaguadentro` / `noelec` / `v14a` | 이진 | 실내 수도 / 전기 없음 / 화장실 |
| `rooms` / `overcrowding` / `hacdor` | 수치 / 이진 | 방 수 / 과밀도 / 과밀 여부 |
| `tamhog` / `hogar_nin` / `hogar_mayor` | 수치 | 가구원 수 / 아동 수 / 고령 수 |
| `refrig` / `computer` / `television` / `v18q` | 이진 | 냉장고 / 컴퓨터 / TV / 태블릿 보유 |
| `qmobilephone` | 수치 | 휴대폰 대수 |
| `meaneduc` / `escolari` | 수치 | 가구 평균 교육연수 / 가구주 교육연수 |
| `age` | 수치 | 가구주 나이 |
| `edjefe` / `edjefa` / `dependency` | 혼합 (**제외**) | 문자·숫자 혼합 변수 — meta 처리 |
| `Id` / `idhogar` | 식별자 (**제외**) | 데이터 누수 차단을 위해 학습에서 배제 |

### *Metrics.*

| 지표 | 의미 |
|---|---|
| **CA** (Classification Accuracy) | 전체 정확도. 클래스 불균형에 취약 |
| **Precision** | 취약이라 예측한 것 중 실제 취약의 비율 |
| **Recall** | 실제 취약 중 모델이 찾아낸 비율. **복지 맥락의 핵심 지표** |
| **F1** | 정밀도와 재현율의 **조화평균** |
| **AUC** | ROC 곡선 아래 면적. 임계값과 무관한 판별력 |
| **MCC** | 매튜스 상관계수. 불균형 데이터에서도 신뢰도 높은 종합 지표 |
| **DI** (Disparate Impact) | 두 집단 유리 결과 비율의 **비(比)**. 1.0이 완전 동등 |
| **SPD** (Statistical Parity Difference) | 두 집단 유리 결과 비율의 **차**. 0이 완전 동등 |
| **EOD** (Equal Opportunity Difference) | 두 집단 **재현율(TPR)** 의 차 |
| **AOD** (Average Odds Difference) | 두 집단 TPR·FPR 차의 평균 |

### *Term.*

- **대리소득조사(PMT)** — 소득을 직접 증빙받기 어려울 때, 주거·자산 등 관측 가능한 특성으로 빈곤을 추정하는 복지 대상 선정 방식
- **계층 추출(Stratified Sampling)** — 클래스·집단 비율을 유지한 채 표본을 나누는 방법. 불균형 데이터의 평가 신뢰도를 지킨다
- **데이터 누수(Data Leakage)** — 예측 시점에 알 수 없거나 타깃 정보를 담은 변수(식별자 등)가 학습에 섞여 성능이 부풀려지는 현상
- **위음성 / 위양성** — 실제 취약인데 비취약으로 판정(지원 배제) / 실제 비취약인데 취약으로 판정(과다 지원). 복지에서는 **위음성의 비용이 더 크다**
- **정보 이득 / 엔트로피** — 결정 트리가 분기 기준을 고를 때 쓰는 척도. 분할 전후 불순도의 감소량
- **배깅(Bagging)** — 서로 다른 부분집합으로 학습한 여러 모델의 예측을 다수결·평균으로 종합하는 앙상블 기법
- **시그모이드 함수** — 실수값을 0~1의 확률로 압축하는 함수. 로지스틱 회귀의 출력단
- **보호속성(Protected Attribute)** — 차별이 발생해선 안 되는 기준 변수(성별·지역 등)
- **특권집단(Privileged Group)** — 공정성 지표 계산의 기준이 되는, 유리한 결과를 더 많이 받는 집단
- **Dataset Bias** — 모델 학습 **이전** 데이터 자체의 집단 편향을 측정하는 Orange Fairness 위젯
- **Reweighing** — 학습 전 각 샘플에 가중치를 재부여해 집단 간 불균형을 보정하는 **전처리** 기반 편향 완화 기법
- **Weighted Logistic Regression** — Reweighing 가중치를 반영해 학습한 로지스틱 회귀. 본 탐구의 편향 완화 모델
- **공정성 trade-off** — 공정성 지표를 개선하면 정확도가 하락하고, 여러 공정성 기준을 동시에 만족시킬 수는 없는 구조적 제약

</details>
