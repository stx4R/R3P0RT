# InfGain.

고등학교 과정의 **로그함수**가 머신러닝 기초 알고리즘인 **결정 트리(Decision Tree)** 의 분기 기준을 어떻게 결정하는지를
Play Tennis 데이터셋 14개 샘플로 **전부 손계산**하여 검증한 프로젝트.
섀넌 엔트로피 → 정보 이득 → ID3 트리 완성까지의 과정을 추적하고,
그 결과를 평면 도식이 아닌 **입체 모형**으로 구현해 가설이 트리 구조에서 그대로 관찰됨을 시각·정량적으로 보였다.

---

## *Motive.*

HIU 탐구에서 이진 탐색 알고리즘의 최대 탐색 횟수를 `log₂N` 으로 표현할 수 있다는 것을 알게 되었다.
이 과정에서 로그가 단순히 **탐색 효율을 표현하는 것을 넘어, 인공지능 알고리즘 내부에서도 핵심적인 역할을 한다**는 사실에 흥미를 갖게 되었다.

인공지능학과 진학을 목표로 하는 만큼, 머신러닝의 기초 알고리즘인 결정 트리가 어떤 수학적 원리로 작동하는지 탐구하고 싶었다.
결정 트리는 데이터를 분류할 때 **어떤 기준으로 데이터를 나눌지** 결정해야 하는데, 이 기준을 계산하는 데 고등학교 수학 과정의 로그함수가 **직접** 사용된다.

결정 트리는 스팸 메일 필터링, 의료 진단, 신용 평가 등 다양한 실생활 문제에 활용된다.
이 알고리즘이 최적의 분기 기준을 선택하는 수학적 근거의 핵심에 로그함수가 있다는 사실이 흥미로워 이 주제를 선정하게 되었다.

## *Background.*

### *① 섀넌 엔트로피*

정보 이론의 창시자 클로드 섀넌은 **어떤 사건이 발생할 확률이 낮을수록 그 사건이 담고 있는 정보량이 많다**고 정의했다.

```
H(X) = − Σ p(xᵢ) log₂ p(xᵢ)
```

동전을 던져 앞·뒷면이 각각 1/2 확률로 나올 때 엔트로피는 **1비트로 최댓값**이 되고, 한 면만 100% 나온다면 **H = 0** 이 된다.
이처럼 엔트로피는 데이터의 **불순도**, 즉 혼잡한 정도를 측정하는 지표다.

### *② 정보 이득*

결정 트리는 어떤 특성을 기준으로 데이터를 분기할지 결정할 때, **분기 전후 엔트로피의 감소량**인 정보 이득을 계산하여 가장 효율적인 기준을 선택한다.

```
IG(S, A) = H(S) − Σ ( |Sᵥ| / |S| ) · H(Sᵥ)
```

`S` : 부모 데이터 / `A` : 분기 특성 / `Sᵥ` : `A` 가 값 `v` 인 부분집합

정보량은 `I(x) = log₂(1/p)` 로 정의되어 사건의 확률이 낮을수록 커지며, 엔트로피는 이 **정보량의 기댓값**(`−Σ p log₂ p`)이다.

**밑이 2인 로그를 쓰는 이유**는, 엔트로피를 *"한 샘플의 분류를 알아내기 위해 평균 몇 번의 예/아니오 질문이 필요한가(비트)?"* 로 해석할 수 있기 때문이다.
이 수학적 원리는 HIU 탐구의 이진 탐색에서 `n` 개 후보를 절반씩 줄여 `log₂n` 번 만에 답을 찾던 것과 **같은 원리**다.
또한 한 종류로 확정되면 질문이 필요 없어 `H = 0`, 5:5로 섞이면 한 번의 질문이 필요해 `H = 1비트` 가 된다.

### *③ ID3 알고리즘*

1986년 로스 퀸런이 개발한 ID3 알고리즘은 **정보 이득이 가장 큰 특성을 루트 노드로 선택하고, 하위 노드에 대해 이 과정을 재귀적으로 반복**하여 트리를 완성한다.

## *Research Q & H.*

> 결정 트리가 "가장 좋은 질문"을 고르는 기준은 무엇이며, 그 기준은 로그함수의 어떤 성질에서 나오는가?

- **가설 1 (분기 효율)** — 결정 트리가 데이터를 분기할 때 정보 이득이 가장 높은 특성을 루트 노드로 선택할수록, 더 적은 분기로 데이터를 정확하게 분류할 수 있을 것이다.
- **가설 2 (로그의 성질)** — 엔트로피 공식 `H = −Σ p log₂ p` 에서 로그의 성질에 의해, 데이터가 완전히 한 종류로 구성될수록 엔트로피는 **0에 수렴**하고, 두 종류가 균등하게 섞일수록 엔트로피는 **최댓값(1비트)** 에 가까워질 것이다.

## *Data.*

| 항목 | 내용 |
|---|---|
| 데이터셋 | **Play Tennis Dataset** (Tom M. Mitchell, *Machine Learning*, 1997 · 3장) |
| 규모 | 14개 샘플 |
| 성격 | 기상 조건에 따라 테니스 경기 진행 여부를 판단하는, 결정 트리 설명용 표준 예제 |
| 특성 | `Outlook`, `Temperature`, `Humidity`, `Wind` |
| 값 | Sunny/Overcast/Rain · Hot/Mild/Cool · High/Normal · Weak/Strong |
| 목표 변수 | `Play` (Yes / No) — 전체 Yes 9 / No 5 |

### *Procedure.*

1. 전체 데이터의 루트 엔트로피 `H(S)` 계산
2. 특성 4가지(Outlook, Temperature, Humidity, Wind)에 대해 각각 정보 이득 `IG` 계산
3. `IG` 가 가장 높은 특성을 루트 노드로 선택
4. 선택된 루트 노드 기준으로 하위 분기 엔트로피 계산 및 트리 완성
5. 가설 검증 — 선택된 루트 노드가 데이터를 가장 효율적으로 분류하는지 확인

## *Calculation.*

### *① 루트 엔트로피*

전체 14개 샘플 중 Yes = 9, No = 5 이므로,

```
H(S) = − (9/14) log₂ (9/14) − (5/14) log₂ (5/14) ≈ 0.940 (비트)
```

1에 가까운 값이므로 **데이터의 불순도가 높다**는 점을 확인할 수 있다.

### *② 특성별 정보 이득*

**[ Outlook ]**

| 값 | 샘플 수 | Yes | No | 엔트로피 |
|---|:---:|:---:|:---:|:---:|
| Sunny | 5 | 2 | 3 | 0.971 |
| Overcast | 4 | 4 | 0 | **0** |
| Rain | 5 | 3 | 2 | 0.971 |

```
IG(Outlook) = 0.940 − ( 5/14 × 0.971 + 4/14 × 0 + 5/14 × 0.971 ) = 0.246
```

**[ Humidity ]**

| 값 | 샘플 수 | Yes | No | 엔트로피 |
|---|:---:|:---:|:---:|:---:|
| High | 7 | 3 | 4 | 0.985 |
| Normal | 7 | 6 | 1 | 0.592 |

```
IG(Humidity) = 0.940 − ( 7/14 × 0.985 + 7/14 × 0.592 ) = 0.151
```

**[ Wind ]**

| 값 | 샘플 수 | Yes | No | 엔트로피 |
|---|:---:|:---:|:---:|:---:|
| Weak | 8 | 6 | 2 | 0.811 |
| Strong | 6 | 3 | 3 | **1.000** |

```
IG(Wind) = 0.940 − ( 8/14 × 0.811 + 6/14 × 1.000 ) = 0.048
```

**[ Temperature ]**

| 값 | 샘플 수 | Yes | No | 엔트로피 |
|---|:---:|:---:|:---:|:---:|
| Hot | 4 | 2 | 2 | **1.000** |
| Mild | 6 | 4 | 2 | 0.918 |
| Cool | 4 | 3 | 1 | 0.811 |

```
IG(Temperature) = 0.940 − ( 4/14 × 1.000 + 6/14 × 0.918 + 4/14 × 0.811 ) = 0.029
```

### *③ 정보 이득 순위 및 루트 노드 선택*

| 특성 | 정보 이득 | 순위 |
|---|:---:|:---:|
| **Outlook** | **0.246** | **1위** |
| Humidity | 0.151 | 2위 |
| Wind | 0.048 | 3위 |
| Temperature | 0.029 | 4위 |

Outlook의 정보 이득이 0.246으로 가장 높으므로 **루트 노드로 선택**된다.
특히 `Outlook = Overcast` 의 경우 하위 엔트로피가 **0**으로, 추가 분기 없이 **즉시 Yes로 분류가 완료**된다.

### *④-1. 하위 분기 — Outlook = Sunny*

Sunny 5개(Yes 2, No 3)에 대해 나머지 세 특성의 정보 이득을 다시 계산한다.

| 특성 | 정보 이득 |
|---|:---:|
| **Humidity** | **0.971** (= 0.971 − 0) |
| Temperature | ≈ 0.571 |
| Wind | ≈ 0.020 |

Humidity가 가장 높으므로, Sunny 분기의 **두 번째 분기 기준은 Humidity** 로 결정된다.

### *④-2. 하위 분기 — Outlook = Rain*

Rain에 해당하는 5개 샘플(Yes 3, No 2)에 대해 동일한 절차를 반복한다.
먼저 Rain 서브셋의 엔트로피는

```
H(Rain) = − (3/5) log₂ (3/5) − (2/5) log₂ (2/5) ≈ 0.971
```

로, 1에 가까워 불순도가 높은 상태다. 세 특성으로 분기했을 때의 정보 이득은 다음과 같다.

| 특성 | 분기 결과 | 정보 이득 |
|---|---|:---:|
| **Wind** | Weak 3개(Yes 3, No 0 → H=0) / Strong 2개(Yes 0, No 2 → H=0) | **0.971** |
| Humidity | High 2개(H=1) / Normal 3개(H=0.918) | 0.020 |
| Temperature | Mild 3개(H=0.918) / Cool 2개(H=1) | ≈ 0.020 |

```
IG(Wind | Rain) = 0.971 − ( 3/5 × 0 + 2/5 × 0 ) = 0.971
```

따라서 Wind의 정보 이득이 0.971로 가장 높아, Rain 분기의 두 번째 기준은 **Wind** 로 결정된다.
특히 Wind는 Rain 서브셋을 **Weak(전부 Yes)** 와 **Strong(전부 No)** 으로 완벽히 나누어, 두 하위 노드 모두 엔트로피가 0이 되며 추가 분기 없이 분류가 완료된다.

### *⑤ 완성된 트리*

```
                     [ Outlook ]  H=0.940, IG=0.246
                    /      |      \
              Sunny/   Overcast    \Rain
                  /         |        \
         [ Humidity ]     YES      [ Wind ]
          H=0.971         H=0       H=0.971
          IG=0.971      (n=4)       IG=0.971
          /      \                  /     \
      High      Normal          Weak      Strong
       |          |               |         |
      NO         YES            YES        NO
```

Outlook을 루트로, Sunny 분기는 Humidity, Rain 분기는 Wind를 기준으로 하는 결정 트리가 완성된다.
(Overcast 분기는 엔트로피가 이미 0이므로 즉시 Yes로 분류된다.)

## *Physical Model.*

손으로 계산한 엔트로피와 정보 이득은 단순히 숫자로만 기술되어 **추상적으로 느껴졌다.**
이 수치들이 실제 트리 구조에서 분류를 어떻게 결정하는지, 그리고 *"정보 이득이 가장 높은 특성을 루트로 선택할수록 더 적은 분기로 데이터를 정확히 분류할 수 있다"* 는 가설이 **구조적으로 성립하는지를 눈으로 확인**하기 위해, 평면 도식이 아닌 **입체 모형**으로 구현하였다.

| 모형 요소 | 대응 |
|---|---|
| 구(球) | 트리의 노드 |
| 구를 잇는 이쑤시개 | 분기(edge) |
| **빨간 구** | 엔트로피 > 0 — 아직 분류가 끝나지 않아 추가 분기가 필요한 **내부 노드** |
| **파란 구** | 엔트로피 = 0 — 한 종류로 완전히 분류된 **잎 노드** |
| 간선 라벨 | 분기값 (Sunny/Overcast/Rain, High/Normal, Weak/Strong) |
| 노드 라벨 | 특성명 + 그 노드의 엔트로피 `H` + 해당 지점의 정보 이득 `IG` |
| 잎 라벨 | 도달한 표본 수 `n` + 루트부터의 **누적 정보 이득** |

### *Cumulative IG.*

| 경로 | 분기 횟수 | 누적 정보 이득 | 도달 |
|---|:---:|:---:|---|
| Overcast | **1회** | **0.246** | 파란 잎 (Yes) |
| Sunny → Humidity | 2회 | **1.217** | 파란 잎 (No / Yes) |
| Rain → Wind | 2회 | **1.217** | 파란 잎 (Yes / No) |

정보 이득이 가장 큰 Outlook을 루트로 택한 결과, **한 분기(Overcast)는 즉시 분류되고 나머지 두 경로만 추가 계산이 필요**했음을 보여준다.
색(파란 잎 도달)과 누적 정보 이득(**0.246 대 1.217**)이 함께, 가설이 트리 구조에서 그대로 관찰됨을 시각적·정량적으로 보여준다.

### *Difference.*

기존의 평면 결정 트리 도식과 두 가지 차별점을 두었다.

1. **엔트로피의 색 부호화** — 상자에 숫자만 적는 일반 도식과 달리, 노드의 엔트로피를 빨강·파랑으로 부호화하여 **불순도와 완전 분류 여부가 한눈에 인식**되도록 하였다.
2. **누적 정보 이득 표기** — 노드별 정보 이득만 표시하는 표준 시각화와 달리, 루트부터 각 잎까지의 정보 이득을 **누적해 표기**함으로써 각 경로가 완전 분류에 도달하기까지 필요한 **총 정보량과 분기 깊이를 직접 비교**할 수 있게 하였다. 이 누적 정보 이득은 일반 결정 트리 그림에는 없는 **독자적인 분석 지표**다.

## *Result.*

| 특성 | 정보 이득 |
|---|:---:|
| **Outlook** | **0.246 (최댓값)** |
| Humidity | 0.151 |
| Wind | 0.048 |
| Temperature | 0.029 |

- 루트 엔트로피 **0.940비트** — 1에 가까워 초기 불순도가 높은 상태임을 확인.
- 정보 이득이 가장 큰 **Outlook** 이 루트 노드로 선택되었고, `Overcast` 인 경우 하위 엔트로피가 0이 되어 **추가 분기 없이 즉시 분류 완료**.
- `Sunny` 분기에서 재계산한 결과 **Humidity(0.971)** 가, `Rain` 분기에서는 **Wind(0.971)** 가 각각 두 번째 기준으로 선택됨.
- Wind는 Rain 서브셋을 두 하위 노드 모두 엔트로피 0으로 완벽히 갈랐다.

→ **정보 이득이 높은 특성을 선택할수록 더 적은 분기로 데이터를 정확히 분류할 수 있다**는 **가설 1이 검증**되었다.
→ 데이터가 한 종류로만 구성될 때 `H = 0`(Overcast, Rain-Weak, Rain-Strong), 두 종류가 균등하게 섞일 때 `H = 1.000`(Wind-Strong, Temperature-Hot)에 도달하여 **가설 2도 함께 검증**되었다.

## *Conclusion*

1. **엔트로피 공식은 로그함수 그 자체다.** `H = −Σ p log₂ p` 를 직접 계산하면서, 로그함수의 성질이 "한 종류면 0, 균등하면 최댓값"이라는 불순도 척도의 성질과 정확히 대응한다는 것을 확인하였다.
2. **밑 2의 의미는 '질문 횟수'다.** 엔트로피를 *"한 샘플을 분류하는 데 평균 몇 번의 예/아니오 질문이 필요한가"* 로 읽으면, HIU 탐구의 이진 탐색 `log₂n` 과 같은 원리임이 드러난다. 로그는 서로 다른 두 문제를 잇는 공통 언어였다.
3. **로그는 계산을 빠르게 하는 함수가 아니라, 불확실성을 정량화하는 도구다.** 이 점이 가장 인상 깊었다.
4. **'AI가 학습한다'의 실체를 확인했다.** 평소 추상적으로 느껴졌던 이 표현이, 실은 **매 분기마다 엔트로피를 비교하며 가장 정보량이 큰 선택을 반복하는 수학적 과정**이라는 것을 손계산으로 체감할 수 있었다.
5. **수학은 입시 과목이 아니라 인공지능 분야의 기반 언어다.** 고등학교 교과 과정의 로그함수가 최신 인공지능 기술과 직접 연결된다는 사실에서, 진로에 대해 더 깊게 탐구하고 생각해보는 시간이 되었다.

## *Limit & Plan*

- **① 다른 분기 지표 비교** — ID3의 정보 이득 외에, CART 알고리즘의 **지니 불순도**나 C4.5의 **정보 이득 비율(Gain Ratio)** 을 비교하여 각 지표가 트리 구조에 어떤 차이를 만드는지 탐구한다.
- **② 실제 코드 구현** — Python의 `scikit-learn` 을 활용해 동일한 데이터셋으로 결정 트리를 직접 구현하고, **손으로 계산한 결과와 일치하는지 검증**한다.
- **③ 과적합 문제 탐구** — 트리가 너무 깊어질 때 발생하는 **과적합** 현상과 이를 막는 **가지치기(pruning)** 기법을 추가로 학습하여 결정 트리의 한계와 보완 방법까지 이해한다.

---

<details>
<summary><b>*Appendix.*</b></summary>

### *Main Variables.*

| 변수 | 유형 | 설명 |
|---|---|---|
| `Outlook` | 범주 | 날씨 개황 — Sunny / Overcast / Rain |
| `Temperature` | 범주 | 기온 — Hot / Mild / Cool |
| `Humidity` | 범주 | 습도 — High / Normal |
| `Wind` | 범주 | 바람 — Weak / Strong |
| `Play` | 범주 (타깃) | 경기 진행 여부 — Yes / No |
| `H(S)` | 수치 (파생) | 부모 노드의 엔트로피 (비트) |
| `H(Sᵥ)` | 수치 (파생) | 값 `v` 부분집합의 엔트로피 |
| `IG(S, A)` | 수치 (파생) | 특성 `A` 로 분기했을 때의 정보 이득 |
| 누적 IG | 수치 (파생·독자) | 루트부터 해당 잎까지 정보 이득의 합 |

### *Entropy Reference.*

| 구성 (Yes : No) | 엔트로피 | 의미 |
|---|:---:|---|
| 4 : 0 / 0 : 2 / 3 : 0 | **0** | 완전 분류 — 질문 불필요 (잎 노드) |
| 6 : 1 | 0.592 | 불순도 낮음 |
| 3 : 1 / 6 : 2 | 0.811 | — |
| 4 : 2 | 0.918 | — |
| 9 : 5 | 0.940 | 루트 노드 |
| 2 : 3 / 3 : 2 | 0.971 | 1에 근접 — 불순도 높음 |
| 3 : 4 | 0.985 | — |
| 2 : 2 / 3 : 3 | **1.000** | 완전 균등 — 최댓값 (1비트) |

### *Term.*

- **정보량 `I(x) = log₂(1/p)`** — 확률이 낮은 사건일수록 커지는 값. "얼마나 놀라운 사건인가"
- **섀넌 엔트로피** — 정보량의 기댓값. 데이터의 불순도(혼잡한 정도)를 비트 단위로 측정
- **비트(bit)** — 예/아니오 질문 한 번에 해당하는 정보 단위. 밑이 2인 로그를 쓰는 이유
- **불순도(Impurity)** — 한 노드 안에 서로 다른 클래스가 섞여 있는 정도
- **정보 이득(IG)** — 분기 전후 엔트로피의 감소량. 클수록 좋은 질문
- **ID3** — 1986년 로스 퀸런이 개발한, 정보 이득 기준으로 트리를 재귀 구성하는 알고리즘
- **루트 노드 / 내부 노드 / 잎 노드** — 트리의 시작점 / 추가 분기가 필요한 노드(H > 0) / 분류가 끝난 노드(H = 0)
- **누적 정보 이득** — 루트부터 잎까지 각 분기의 IG를 합산한 값. 본 탐구의 독자적 분석 지표
- **가지치기(Pruning)** — 과적합을 막기 위해 트리의 일부 가지를 제거하는 기법
- **지니 불순도 / 정보 이득 비율** — 각각 CART, C4.5가 정보 이득 대신 사용하는 분기 기준

### *References.*

**[ 도서 ]**
- 제이 웬그로우, 『누구나 자료 구조와 알고리즘』, 이민석 역, 한빛미디어, 2018 (p. 85~92) → 이진 탐색 알고리즘 및 로그 복잡도 분석 참고
- Tom M. Mitchell, *Machine Learning*, McGraw-Hill, 1997 (3장 Decision Tree Learning) → Play Tennis 예제 데이터셋 및 결정 트리 학습 참고

**[ 학술자료 ]**
- Ross Quinlan, "Induction of Decision Trees", *Machine Learning*, Vol. 1, 1986 → ID3 알고리즘 개발 원논문

**[ 인터넷 ]**
- UCI Machine Learning Repository — Play Tennis Dataset · https://archive.ics.uci.edu/ml/datasets/
- Wikipedia — Shannon Entropy · https://en.wikipedia.org/wiki/Entropy_(information_theory)
- Wikipedia — Information Gain (decision tree) · https://en.wikipedia.org/wiki/Information_gain_in_decision_trees

</details>
